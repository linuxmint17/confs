[user]
    name = wangshaodong
    email = shaodongwang@foxmail.com
[core]
    editor = vi
[credential]
    helper = store
[alias]
    co = checkout
    br = branch
    ci = commit
    st = status
    pu = push
    unstage = reset HEAD --
    hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
    last = log -1 HEAD
    type = cat-file -t
    dump = cat-file -p
[commit]
    template = ~/.git-commit-template.txt
